{
  "name": "catalog-microservice",
  "version": "1.0.0",
  "description": "A micro service which does all the CRUD operations for catalog",
  "main": "Catalog.js",
  "scripts": {
    "test": "mocha --timeout 10000",
    "test_junit": "mocha test --reporter mocha-junit-reporter --timeout 10000 --reporter-options mochaFile=./test/test_results.xml"
  },
  "keywords": [
    "ecommerce",
    "micro-service",
    "microservices"
  ],
  "author": "Consort Toolchain",
  "dependencies": {
    "axios": "^0.21.4",
    "body-parser": "^1.19.1",
    "cors": "^2.8.5",
    "express": "^4.17.2",
    "mongoose": "^5.13.13",
    "multer": "^1.4.4"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "chai-files": "^1.4.0",
    "chai-http": "^2.0.1",
    "mocha": "^2.4.5",
    "mocha-junit-reporter": "^2.0.2"
  }
}
